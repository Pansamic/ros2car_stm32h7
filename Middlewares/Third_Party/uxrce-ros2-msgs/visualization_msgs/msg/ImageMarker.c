// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file ImageMarker.c
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#include "visualization_msgs/msg/ImageMarker.h"

#include <ucdr/microcdr.h>
#include <string.h>








bool visualization_msgs_msg_ImageMarker_serialize_topic(ucdrBuffer* writer, const visualization_msgs_msg_ImageMarker* topic)
{
    bool success = true;

        success &= std_msgs_msg_Header_serialize_topic(writer, &topic->header);
        success &= ucdr_serialize_string(writer, topic->ns);

        success &= ucdr_serialize_int32_t(writer, topic->id);

        success &= ucdr_serialize_int32_t(writer, topic->type);

        success &= ucdr_serialize_int32_t(writer, topic->action);

        success &= geometry_msgs_msg_Point_serialize_topic(writer, &topic->position);
        success &= ucdr_serialize_float(writer, topic->scale);

        success &= std_msgs_msg_ColorRGBA_serialize_topic(writer, &topic->outline_color);
        success &= ucdr_serialize_uint8_t(writer, topic->filled);

        success &= std_msgs_msg_ColorRGBA_serialize_topic(writer, &topic->fill_color);
        success &= builtin_interfaces_msg_Duration_serialize_topic(writer, &topic->lifetime);
        success &= ucdr_serialize_uint32_t(writer, topic->points_size);
        for(size_t i = 0; i < topic->points_size; ++i)
        {
            success &= geometry_msgs_msg_Point_serialize_topic(writer, &topic->points[i]);
        }

        success &= ucdr_serialize_uint32_t(writer, topic->outline_colors_size);
        for(size_t i = 0; i < topic->outline_colors_size; ++i)
        {
            success &= std_msgs_msg_ColorRGBA_serialize_topic(writer, &topic->outline_colors[i]);
        }

    return success && !writer->error;
}

bool visualization_msgs_msg_ImageMarker_deserialize_topic(ucdrBuffer* reader, visualization_msgs_msg_ImageMarker* topic)
{
    bool success = true;

        success &= std_msgs_msg_Header_deserialize_topic(reader, &topic->header);
        success &= ucdr_deserialize_string(reader, topic->ns, 255);

        success &= ucdr_deserialize_int32_t(reader, &topic->id);

        success &= ucdr_deserialize_int32_t(reader, &topic->type);

        success &= ucdr_deserialize_int32_t(reader, &topic->action);

        success &= geometry_msgs_msg_Point_deserialize_topic(reader, &topic->position);
        success &= ucdr_deserialize_float(reader, &topic->scale);

        success &= std_msgs_msg_ColorRGBA_deserialize_topic(reader, &topic->outline_color);
        success &= ucdr_deserialize_uint8_t(reader, &topic->filled);

        success &= std_msgs_msg_ColorRGBA_deserialize_topic(reader, &topic->fill_color);
        success &= builtin_interfaces_msg_Duration_deserialize_topic(reader, &topic->lifetime);
        success &= ucdr_deserialize_uint32_t(reader, &topic->points_size);
        if(topic->points_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->points_size; ++i)
            {
                success &= geometry_msgs_msg_Point_deserialize_topic(reader, &topic->points[i]);
            }
        }

        success &= ucdr_deserialize_uint32_t(reader, &topic->outline_colors_size);
        if(topic->outline_colors_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->outline_colors_size; ++i)
            {
                success &= std_msgs_msg_ColorRGBA_deserialize_topic(reader, &topic->outline_colors[i]);
            }
        }

    return success && !reader->error;
}

uint32_t visualization_msgs_msg_ImageMarker_size_of_topic(const visualization_msgs_msg_ImageMarker* topic, uint32_t size)
{
    uint32_t previousSize = size;
        size += std_msgs_msg_Header_size_of_topic(&topic->header, size);
        size += ucdr_alignment(size, 4) + 4 + (uint32_t)strlen(topic->ns) + 1;

        size += ucdr_alignment(size, 4) + 4;

        size += ucdr_alignment(size, 4) + 4;

        size += ucdr_alignment(size, 4) + 4;

        size += geometry_msgs_msg_Point_size_of_topic(&topic->position, size);
        size += ucdr_alignment(size, 4) + 4;

        size += std_msgs_msg_ColorRGBA_size_of_topic(&topic->outline_color, size);
        size += ucdr_alignment(size, 1) + 1;

        size += std_msgs_msg_ColorRGBA_size_of_topic(&topic->fill_color, size);
        size += builtin_interfaces_msg_Duration_size_of_topic(&topic->lifetime, size);
        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->points_size; ++i)
        {
            size += geometry_msgs_msg_Point_size_of_topic(&topic->points[i], size);
        }

        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->outline_colors_size; ++i)
        {
            size += std_msgs_msg_ColorRGBA_size_of_topic(&topic->outline_colors[i], size);
        }

    return size - previousSize;
}
