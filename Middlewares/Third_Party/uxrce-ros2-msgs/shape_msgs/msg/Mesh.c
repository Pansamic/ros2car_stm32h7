// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file Mesh.c
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#include "shape_msgs/msg/Mesh.h"

#include <ucdr/microcdr.h>
#include <string.h>

bool shape_msgs_msg_Mesh_serialize_topic(ucdrBuffer* writer, const shape_msgs_msg_Mesh* topic)
{
    bool success = true;

        success &= ucdr_serialize_uint32_t(writer, topic->triangles_size);
        for(size_t i = 0; i < topic->triangles_size; ++i)
        {
            success &= shape_msgs_msg_MeshTriangle_serialize_topic(writer, &topic->triangles[i]);
        }

        success &= ucdr_serialize_uint32_t(writer, topic->vertices_size);
        for(size_t i = 0; i < topic->vertices_size; ++i)
        {
            success &= geometry_msgs_msg_Point_serialize_topic(writer, &topic->vertices[i]);
        }

    return success && !writer->error;
}

bool shape_msgs_msg_Mesh_deserialize_topic(ucdrBuffer* reader, shape_msgs_msg_Mesh* topic)
{
    bool success = true;

        success &= ucdr_deserialize_uint32_t(reader, &topic->triangles_size);
        if(topic->triangles_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->triangles_size; ++i)
            {
                success &= shape_msgs_msg_MeshTriangle_deserialize_topic(reader, &topic->triangles[i]);
            }
        }

        success &= ucdr_deserialize_uint32_t(reader, &topic->vertices_size);
        if(topic->vertices_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->vertices_size; ++i)
            {
                success &= geometry_msgs_msg_Point_deserialize_topic(reader, &topic->vertices[i]);
            }
        }

    return success && !reader->error;
}

uint32_t shape_msgs_msg_Mesh_size_of_topic(const shape_msgs_msg_Mesh* topic, uint32_t size)
{
    uint32_t previousSize = size;
        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->triangles_size; ++i)
        {
            size += shape_msgs_msg_MeshTriangle_size_of_topic(&topic->triangles[i], size);
        }

        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->vertices_size; ++i)
        {
            size += geometry_msgs_msg_Point_size_of_topic(&topic->vertices[i], size);
        }

    return size - previousSize;
}
