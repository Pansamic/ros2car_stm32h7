// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file MetricsMessage.c
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#include "statistics_msgs/msg/MetricsMessage.h"

#include <ucdr/microcdr.h>
#include <string.h>

bool statistics_msgs_msg_MetricsMessage_serialize_topic(ucdrBuffer* writer, const statistics_msgs_msg_MetricsMessage* topic)
{
    bool success = true;

        success &= ucdr_serialize_string(writer, topic->measurement_source_name);

        success &= ucdr_serialize_string(writer, topic->metrics_source);

        success &= ucdr_serialize_string(writer, topic->unit);

        success &= builtin_interfaces_msg_Time_serialize_topic(writer, &topic->window_start);
        success &= builtin_interfaces_msg_Time_serialize_topic(writer, &topic->window_stop);
        success &= ucdr_serialize_uint32_t(writer, topic->statistics_size);
        for(size_t i = 0; i < topic->statistics_size; ++i)
        {
            success &= statistics_msgs_msg_StatisticDataPoint_serialize_topic(writer, &topic->statistics[i]);
        }

    return success && !writer->error;
}

bool statistics_msgs_msg_MetricsMessage_deserialize_topic(ucdrBuffer* reader, statistics_msgs_msg_MetricsMessage* topic)
{
    bool success = true;

        success &= ucdr_deserialize_string(reader, topic->measurement_source_name, 255);

        success &= ucdr_deserialize_string(reader, topic->metrics_source, 255);

        success &= ucdr_deserialize_string(reader, topic->unit, 255);

        success &= builtin_interfaces_msg_Time_deserialize_topic(reader, &topic->window_start);
        success &= builtin_interfaces_msg_Time_deserialize_topic(reader, &topic->window_stop);
        success &= ucdr_deserialize_uint32_t(reader, &topic->statistics_size);
        if(topic->statistics_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->statistics_size; ++i)
            {
                success &= statistics_msgs_msg_StatisticDataPoint_deserialize_topic(reader, &topic->statistics[i]);
            }
        }

    return success && !reader->error;
}

uint32_t statistics_msgs_msg_MetricsMessage_size_of_topic(const statistics_msgs_msg_MetricsMessage* topic, uint32_t size)
{
    uint32_t previousSize = size;
        size += ucdr_alignment(size, 4) + 4 + (uint32_t)strlen(topic->measurement_source_name) + 1;

        size += ucdr_alignment(size, 4) + 4 + (uint32_t)strlen(topic->metrics_source) + 1;

        size += ucdr_alignment(size, 4) + 4 + (uint32_t)strlen(topic->unit) + 1;

        size += builtin_interfaces_msg_Time_size_of_topic(&topic->window_start, size);
        size += builtin_interfaces_msg_Time_size_of_topic(&topic->window_stop, size);
        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->statistics_size; ++i)
        {
            size += statistics_msgs_msg_StatisticDataPoint_size_of_topic(&topic->statistics[i], size);
        }

    return size - previousSize;
}
