// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file JointState.c
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#include "sensor_msgs/msg/JointState.h"

#include <ucdr/microcdr.h>
#include <string.h>

bool sensor_msgs_msg_JointState_serialize_topic(ucdrBuffer* writer, const sensor_msgs_msg_JointState* topic)
{
    bool success = true;

        success &= std_msgs_msg_Header_serialize_topic(writer, &topic->header);
        success &= ucdr_serialize_uint32_t(writer, topic->name_size);
        for(size_t i = 0; i < topic->name_size; ++i)
        {
            success &= ucdr_serialize_string(writer, topic->name[i]);
        }

        success &= ucdr_serialize_sequence_double(writer, topic->position, topic->position_size);

        success &= ucdr_serialize_sequence_double(writer, topic->velocity, topic->velocity_size);

        success &= ucdr_serialize_sequence_double(writer, topic->effort, topic->effort_size);

    return success && !writer->error;
}

bool sensor_msgs_msg_JointState_deserialize_topic(ucdrBuffer* reader, sensor_msgs_msg_JointState* topic)
{
    bool success = true;

        success &= std_msgs_msg_Header_deserialize_topic(reader, &topic->header);
        success &= ucdr_deserialize_uint32_t(reader, &topic->name_size);
        if(topic->name_size > 100)
        {
            reader->error = true;
        }
        else
        {
            for(size_t i = 0; i < topic->name_size; ++i)
            {
                success &= ucdr_deserialize_string(reader, topic->name[i], 255);
            }
        }

        success &= ucdr_deserialize_sequence_double(reader, topic->position, 100, &topic->position_size);

        success &= ucdr_deserialize_sequence_double(reader, topic->velocity, 100, &topic->velocity_size);

        success &= ucdr_deserialize_sequence_double(reader, topic->effort, 100, &topic->effort_size);

    return success && !reader->error;
}

uint32_t sensor_msgs_msg_JointState_size_of_topic(const sensor_msgs_msg_JointState* topic, uint32_t size)
{
    uint32_t previousSize = size;
        size += std_msgs_msg_Header_size_of_topic(&topic->header, size);
        size += ucdr_alignment(size, 4) + 4;
        for(size_t i = 0; i < topic->name_size; ++i)
        {
            size += ucdr_alignment(size, 4) + 4 + (uint32_t)strlen(topic->name[i]) + 1;
        }

        size += ucdr_alignment(size, 4) + 4;
        size += ucdr_alignment(size, 8) + topic->position_size * 8;

        size += ucdr_alignment(size, 4) + 4;
        size += ucdr_alignment(size, 8) + topic->velocity_size * 8;

        size += ucdr_alignment(size, 4) + 4;
        size += ucdr_alignment(size, 8) + topic->effort_size * 8;

    return size - previousSize;
}
